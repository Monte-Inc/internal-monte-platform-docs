---
title: "Runs and lineages"
description: "How eval and train executions are tracked and chained"
---

## Runs

- What a Run is: one eval or train execution recorded in the Ledger
- Run references: how to name a run in `monte show` / `monte stop` / `monte promote`

## Lineages

- A Lineage is a chain of training runs building on one another
- `--chained` — continuing from a prior checkpoint
- Per-lineage delta in `monte status`

## Run lifecycle

- Launch, monitor (`monte logs`), stop (`monte stop`), inspect (`monte show`)
- Exit codes and refusal semantics

## Related pages

- Ledger, Measurement

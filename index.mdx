---
title: "Introduction"
---

<img src="/images/monte-launch.png" alt="Monte" noZoom />

**Monte is the agent-native platform for post-training.**

AI agents can handle the individual tasks of post-training research well. They can change a training method, launch a run, or inspect an evaluation. They are much less reliable at managing the improvement process as a whole.

An agent may become overly convinced of an early diagnosis and keep working from it after the evidence points in a different direction. It can chase a single metric and lose the broader goal. It may compare results produced under different conditions, repeat work it already did, or stick with an approach that stopped paying off. At each step it looks productive, while the process as a whole degrades.

Monte gives agents a consistent view of that process. It records the context behind each result and how the work changes over time, so an agent can revisit a decision and choose what to try next.

Monte fixes what has to be held still for a comparison to mean anything. Results are compared only against others produced the same way: the same starting model, the same frozen tasks, the same evaluation settings. Each such group carries its own baseline, and a comparison never crosses between groups. What Monte does not constrain is the training method or the research strategy.

## How it works

Monte holds what each training Run did, what each eval scored, and how a model performs on a given Task.

An Environment carries what you need to score a Task: the Tasks, their answers, and the grader. An exam Environment holds a published benchmark, and a curriculum Environment holds a training pile. One Environment can serve several Experiments. `monte experiment init` creates an Experiment over a set of exams and fixes each one's role: **visible** exams the loop scores freely and reasons over, and **sealed** exams the loop can never evaluate, held back for the final claim.

An Experiment starts from a base model, which can be any model. You score that base model on every exam before any training: a visible exam's Baseline is what later Runs are read against, and a sealed exam's Baseline anchors the final claim. Then you train and score each new Checkpoint against the Baseline. A Recipe states how a Run trains: the algorithm, the optimizer, the batch settings, and how it is distributed across GPUs. Each Run records the Recipe it ran and the Source it trained on, so the method behind a number is never lost. Monte writes the run plan, events, and training logs in the same shape for every Run. You can see from them which settings to change next.

Everything in the box below happens under one Experiment.

```mermaid actions={false}
%%{init: {'theme': 'base', 'themeVariables': {'primaryColor': '#315594', 'primaryTextColor': '#F0F1F2', 'primaryBorderColor': '#315594', 'lineColor': '#70A6C1', 'textColor': '#17181B', 'clusterBkg': 'transparent', 'clusterBorder': '#70A6C1', 'edgeLabelBackground': 'transparent', 'tertiaryColor': '#315594', 'tertiaryTextColor': '#F0F1F2', 'tertiaryBorderColor': '#315594'}, 'flowchart': {'subGraphTitleMargin': {'top': 0, 'bottom': 12}, 'rankSpacing': 70, 'nodeSpacing': 55, 'padding': 18}}}%%
flowchart TD
    ENV["Environment"]:::entry

    subgraph M[" "]
        BM["Base model"]:::model
        BASE["Baseline"]:::score
        CP["Checkpoint"]:::model
        SCORE["Checkpoint score"]:::score
        FINAL["Final scores, one per sealed exam"]:::score

        BM -->|"monte eval"| BASE
        BM -->|"monte train"| CP
        CP -->|"monte eval"| SCORE
        SCORE -->|"monte train"| CP
        SCORE -->|"nominate, then the blind claim"| FINAL
    end

    ENV -->|"monte experiment init"| BM
    FINAL -->|"monte promote"| PROM(["Promoted weights"]):::promoted
    FINAL -->|"no improvement"| NONE(["Nothing promoted"]):::none

    classDef entry fill:#2F3675,stroke:#70A6C1,stroke-width:1.5px,color:#F0F1F2;
    classDef model fill:#70A6C1,stroke:#70A6C1,stroke-width:1.5px,color:#F0F1F2;
    classDef score fill:#2F3675,stroke:#70A6C1,stroke-width:1.5px,color:#F0F1F2;
    classDef promoted fill:#70A6C1,stroke:#70A6C1,stroke-width:1.5px,color:#F0F1F2;
    classDef none fill:#1E2024,stroke:#4A4D50,stroke-width:1.5px,color:#F0F1F2;
```

Each sealed exam is read twice: once on the base model before any training, and once at the end, on the Checkpoint the search nominates. Those final reads are the numbers you report — one score per sealed exam, and nothing merges them into one. `monte promote` copies a Checkpoint's weights out with its provenance, behind a confirmation you type in full. Every Run, score, and cost is recorded in that same shape. The driving agent reads that record, runs the loop, and decides what to try next.

## Start here

<CardGroup cols={3}>
  <Card title="Quickstart" href="/quickstart">
    Run the loop on your laptop in minutes. No GPU.
  </Card>

  <Card title="Improvement" href="/concepts/improvement">
    How training becomes evidence of improvement.
  </Card>

  <Card title="CLI reference" href="/cli/overview">
    Every command, flag, and exit code.
  </Card>
</CardGroup>
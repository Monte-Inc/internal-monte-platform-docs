---
title: "Introduction"
description: "What Monte is and why it exists"
---

Monte is our internal platform for one job: make a model measurably better at a graded task set, and prove it. You define a Measurement. The loop returns a better model or an honest null result. Nothing else counts as an outcome.

<Note>
Monte is in closed beta. Interfaces and these docs change quickly, and commands can break between versions.
</Note>

## How it works

The `monte` CLI drives everything on a single GPU box. Training and evaluation run through NVIDIA NeMo: Gym for rollouts and grading, NeMo-RL for training. Every Run lands as a row in an append-only Ledger. You read scores as paired deltas against a frozen Baseline, never as bare numbers.

## The pieces

| Piece | What it is |
|---|---|
| CLI | The `monte` command. The only write path. |
| Environments | Installable packages of graded tasks. Each one owns its grader. |
| Measurements | Frozen definitions of what is measured and how. |
| Ledger | The append-only record of every Run. The single source of truth. |
| Console | A read-only web view over the same Ledger. |

## Start here

<CardGroup cols={3}>
  <Card title="Quickstart" href="/quickstart">
    Run the loop on your laptop in minutes. No GPU.
  </Card>
  <Card title="The loop" href="/concepts/loop">
    How training becomes evidence of improvement.
  </Card>
  <Card title="CLI reference" href="/cli/overview">
    Every command, flag, and exit code.
  </Card>
</CardGroup>
